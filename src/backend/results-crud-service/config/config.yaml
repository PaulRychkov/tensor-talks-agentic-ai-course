server:
  host: "0.0.0.0"
  port: 8088

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: ""
  name: "results_crud_db"
  ssl_mode: "disable"
  schema: ""  # Empty for local dev (separate DB), set to "results_crud" for Kubernetes
