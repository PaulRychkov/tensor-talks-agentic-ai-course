server:
  host: "0.0.0.0"
  port: 8085

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: ""
  name: "session_crud_db"
  ssl_mode: "disable"
  schema: ""  # Empty for local dev (separate DB), set to "session_crud" for Kubernetes
