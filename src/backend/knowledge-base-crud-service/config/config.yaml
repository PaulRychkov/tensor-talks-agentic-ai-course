server:
  host: "0.0.0.0"
  port: 8090

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: ""
  name: "knowledge_base_crud_db"
  ssl_mode: "disable"
  schema: ""  # Empty for local dev (separate DB), set to "knowledge_base_crud" for Kubernetes

