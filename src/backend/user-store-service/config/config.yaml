server:
  host: "0.0.0.0"
  port: 8082

database:
  host: "postgres"
  port: 5432
  user: "user_store"
  password: "user_store_password"
  name: "user_store_db"
  ssl_mode: "disable"
  schema: ""  # Empty for local dev (separate DB), set to "user_store" for Kubernetes

