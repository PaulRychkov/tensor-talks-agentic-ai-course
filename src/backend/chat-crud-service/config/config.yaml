server:
  host: "0.0.0.0"
  port: 8087

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: ""
  name: "chat_crud_db"
  ssl_mode: "disable"
  schema: ""  # Empty for local dev (separate DB), set to "chat_crud" for Kubernetes
