{{- if and (index .Values.services "session-service").enabled (index .Values.services "session-service").autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "tensor-talks.fullname" . }}-session-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "tensor-talks.labels" . | nindent 4 }}
    app.kubernetes.io/component: session-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "tensor-talks.fullname" . }}-session-service
  minReplicas: {{ (index .Values.services "session-service").autoscaling.minReplicas }}
  maxReplicas: {{ (index .Values.services "session-service").autoscaling.maxReplicas }}
  metrics:
    {{- if (index .Values.services "session-service").autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ (index .Values.services "session-service").autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if (index .Values.services "session-service").autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ (index .Values.services "session-service").autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}

