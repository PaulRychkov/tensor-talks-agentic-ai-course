server:
  host: "0.0.0.0"
  port: 8091

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: ""
  name: "questions_crud_db"
  ssl_mode: "disable"
  schema: ""  # Empty for local dev (separate DB), set to "questions_crud" for Kubernetes

