{{- if (index .Values.services "knowledge-base-crud-service").enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tensor-talks.fullname" . }}-knowledge-base-crud-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "tensor-talks.labels" . | nindent 4 }}
    app.kubernetes.io/component: knowledge-base-crud-service
spec:
  replicas: {{ (index .Values.services "knowledge-base-crud-service").replicaCount }}
  selector:
    matchLabels:
      {{- include "tensor-talks.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: knowledge-base-crud-service
  template:
    metadata:
      labels:
        {{- include "tensor-talks.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: knowledge-base-crud-service
        team: tensor-talks
        project: tensor-talks
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: {{ include "tensor-talks.serviceAccountName" . }}
{{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: knowledge-base-crud-service
          image: {{ include "tensor-talks.image" (dict "Values" .Values "repository" (index .Values.services "knowledge-base-crud-service").image.repository "tag" (index .Values.services "knowledge-base-crud-service").image.tag) }}
          imagePullPolicy: {{ (index .Values.services "knowledge-base-crud-service").image.pullPolicy | default (include "tensor-talks.imagePullPolicy" .Values) }}
          ports:
            - name: http
              containerPort: 8090
              protocol: TCP
          env:
            - name: KNOWLEDGE_BASE_CRUD_SERVER_HOST
              value: "0.0.0.0"
            - name: KNOWLEDGE_BASE_CRUD_SERVER_PORT
              value: "8090"
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ include "tensor-talks.fullname" . }}-postgresql
                  key: host
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ include "tensor-talks.fullname" . }}-postgresql
                  key: port
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "tensor-talks.fullname" . }}-postgresql
                  key: user
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "tensor-talks.fullname" . }}-postgresql
                  key: password
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_NAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "tensor-talks.fullname" . }}-postgresql
                  key: database
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_SSL_MODE
              valueFrom:
                secretKeyRef:
                  name: {{ include "tensor-talks.fullname" . }}-postgresql
                  key: sslmode
            - name: KNOWLEDGE_BASE_CRUD_DATABASE_SCHEMA
              value: {{ (index .Values.services "knowledge-base-crud-service").database.schema | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml (index .Values.services "knowledge-base-crud-service").resources | nindent 12 }}
{{- end }}

